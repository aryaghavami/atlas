import { BodyScreen } from "@/components/BodyScreen";

// The one screen. Renders demo numbers until you set up your own (stored on-device).
export default function BodyPage() {
  return <BodyScreen />;
}
