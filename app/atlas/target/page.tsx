import { AtlasSetTarget } from "@/components/AtlasSetTarget";

export default function Page() {
  return (
    <main style={{ minHeight: "100vh", background: "#0a0a0b", display: "flex", alignItems: "center", justifyContent: "center", padding: 40 }}>
      <AtlasSetTarget />
    </main>
  );
}
